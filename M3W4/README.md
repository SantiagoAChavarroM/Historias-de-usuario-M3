# Mini Product App — DOM, LocalStorage & Fetch API 🚀

Santiago Alejandro Chavarro Murillo  
Clan: Thompson  
santiagoachm@gmail.com  
ID: 1152443924  

This project corresponds to **User Story Week 4** of Module 3 at **Riwi**.

The main goal of this project is to build a small web application that integrates **DOM manipulation**, **Local Storage persistence**, and **API consumption using Fetch**, applying modern JavaScript best practices.

---

## 🧩 Technologies Used

- **HTML5** – Semantic structure
- **CSS3** – Styling and layout
- **JavaScript ES6+** – Application logic
- **Vite** – Development environment
- **Local Storage** – Data persistence
- **JSON Server** – REST API simulation
- **Fetch API** – CRUD operations (GET, POST, PUT, DELETE)

---

## 📁 Project Structure

```bash
M3W4/
│
├── index.html
├── app.js
├── style.css
├── db.json
└── public
⚙️ Installation and Usage
Install dependencies

bash
npm install
Run JSON Server

npx json-server db.json --port 3000
Run the project with Vite

npm run dev
Open in the browser:

http://localhost:5173
🧠 Learnings
During this project, the following concepts were reinforced:

Dynamic DOM manipulation using appendChild and removeChild

Proper use of textContent

Data persistence using localStorage

API consumption using async/await and try/catch

Full CRUD implementation with Fetch API

Code organization and clean structure

Offline-first pattern with automatic synchronization